# リリース手順

> **Naruto Shelter Map** のリリース管理ガイド

---

## 📋 概要

このプロジェクトでは、**Semantic Versioning (SemVer)** に従ってバージョン管理を行います。

### バージョン形式

```
v<major>.<minor>.<patch>
例: v0.1.0, v1.0.0, v1.2.3
```

### バージョン番号の意味

- **Major (x.0.0)**: 破壊的変更（API変更、大きな機能変更）
- **Minor (0.x.0)**: 後方互換性のある機能追加
- **Patch (0.0.x)**: バグ修正、小さな改善

---

## 🚀 リリース手順

### Step 1: バージョン番号の更新

`package.json` の `version` フィールドを更新します。

```bash
# 例: 0.1.0 → 0.2.0 (マイナーバージョンアップ)
pnpm version minor

# 例: 0.1.0 → 0.1.1 (パッチバージョンアップ)
pnpm version patch

# 例: 0.1.0 → 1.0.0 (メジャーバージョンアップ)
pnpm version major
```

または、手動で `package.json` を編集：

```json
{
  "version": "0.2.0"
}
```

### Step 2: 変更をコミット

```bash
git add package.json
git commit -m "chore: bump version to 0.2.0"
git push origin main
```

### Step 3: リリースタグの作成

```bash
# package.jsonのバージョンに合わせてタグを作成
VERSION=$(node -p "require('./package.json').version")
git tag -a "v${VERSION}" -m "Release v${VERSION}"
git push origin "v${VERSION}"
```

または、`pnpm version` コマンドを使用（自動的にタグも作成）：

```bash
# バージョン更新 + タグ作成 + コミット（一括）
pnpm version minor --git-tag-version true
git push origin main --tags
```

### Step 4: GitHub Actions による自動リリース

タグをプッシュすると、`.github/workflows/release.yml` が自動的に実行され：

1. ✅ バージョン番号の検証（package.json とタグの一致確認）
2. 📝 リリースノートの自動生成
3. 🚀 GitHub Release の作成

---

## 📝 リリースノート

### 自動生成される内容

- **初回リリース**: 主な機能と技術スタックの概要
- **2回目以降**: 前回リリースからの変更履歴（コミットメッセージから自動生成）

### 手動でリリースノートを編集する場合

GitHub Release の作成後、Web UI から編集可能です。

---

## 🔍 リリース確認

### リリース一覧の確認

```bash
# ローカル
git tag --list

# GitHub
gh release list
```

### 特定のリリースの確認

```bash
gh release view v0.1.0
```

---

## ⚠️ 注意事項

### 1. バージョン番号の整合性

**重要**: `package.json` の `version` と Git タグのバージョン番号は**必ず一致**させる必要があります。

```bash
# 検証コマンド
PACKAGE_VERSION=$(node -p "require('./package.json').version")
TAG_VERSION=$(git describe --tags --abbrev=0 2>/dev/null | sed 's/^v//' || echo "")
if [ "$PACKAGE_VERSION" != "$TAG_VERSION" ]; then
  echo "❌ Version mismatch!"
  exit 1
fi
```

### 2. タグの削除（緊急時のみ）

```bash
# ローカルタグ削除
git tag -d v0.1.0

# リモートタグ削除
git push origin :refs/tags/v0.1.0
```

**注意**: 一度公開したタグは削除しないことを推奨します。

### 3. リリース前のチェックリスト

- [ ] `pnpm lint` が通る
- [ ] `pnpm type-check` が通る
- [ ] `pnpm build` が成功する
- [ ] CI がすべて成功している
- [ ] `package.json` のバージョンが正しい
- [ ] 変更履歴が適切に記録されている

---

## 📚 参考リンク

- [Semantic Versioning](https://semver.org/)
- [GitHub Releases](https://docs.github.com/en/repositories/releasing-projects-on-github)
- [Git Tags](https://git-scm.com/book/en/v2/Git-Basics-Tagging)

---

## 🔄 更新履歴

| 日付 | バージョン | 変更内容 |
|------|-----------|---------|
| 2025-12-01 | 1.0.0 | リリース手順ドキュメント作成 |

